# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Buld and Deploy 
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      # - name: Checkout code
      #   uses: actions/checkout@v3

      # - name: Setup node.js
      #   uses: actions/setup-node@v2
      #   with:
      #     node-version: '14'

      # - name: Install dependencies
      #   run: npm install

      # - name: Build project
      #   run: npm run build

      - name: Deploy Firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_JEFFERSONFREITAS_DEV }}'
          entryPoint: /home/runner/work/jeffersonfreitas.dev/jeffersonfreitas.dev/firebase.json
          channelId: live
          projectId: jeffersonfreitas-dev
